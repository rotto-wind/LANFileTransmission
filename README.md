# 局域网文件传输系统

做这个的起因是不想每次传文件都要通过微信、QQ等第三方软件。目前还是个很小型、简陋的系统，但传一些小文件没有问题。

## 功能

让处在同一个局域网的主机能直接给服务器端主机发送文件。服务器端主机会接收并保存到本地磁盘。

## 使用方法

1. 需要接收文件的用户运行 `server.py` ，得到一个局域网内可访问的 URL；
2. 需要发送文件的用户在浏览器访问这个 URL，选择并上传文件；
3. 若接收成功，文件会保存在项目目录的 `uploads` 文件夹下；
4. 文件传输结束后，接收方通过 `ctrl+c` 终止服务器进程。